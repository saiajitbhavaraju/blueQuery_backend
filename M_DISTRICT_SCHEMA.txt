CREATE TABLE M_DISTRICT (
    DISTRICT_CD NUMBER(10) NOT NULL,
    LANG_CD NUMBER(10) NOT NULL,
    STATE_CD NUMBER(10),
    DISTRICT VARCHAR2(100),
    RECORD_STATUS CHAR(1),
    LAST_UPDATED_ON DATE NOT NULL,
    DIST_SHORT_FORM VARCHAR2(200),
    SEB_FLAG NUMBER(10)
);